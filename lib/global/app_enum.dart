// ignore_for_file: constant_identifier_names

enum TypeCalcalator { increment, decrement }

enum TypeCheckBox { circle, square }

enum TypePress { register, login, forgotPass }

enum TypeMenu { list, doctrine, exam, fund, skill, another }

enum TypeButtonMember { edit, remove, addPoint, attendance, funding }
